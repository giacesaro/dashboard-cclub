import { GET_PARAM_BY_KEY } from "./types";


export function applicationReducer (
    state = {
        
    },
    action
) {
    switch (action.type) {
        case GET_PARAM_BY_KEY:
            
            break;
    
        default:
            return state
    }
}